// PAGE: BERANDA — Today feed

const { useState: useBState } = React;
const CB = window.C;
const { Pill: BPill, Button: BBtn, UserAvatar: BAvatar, ProgressBar: BProgress, NavTabs: BTabs, Modal: BModal } = window;

const today = new Date();
const HARI  = ['Minggu','Senin','Selasa','Rabu','Kamis','Jumat','Sabtu'][today.getDay()];
const BULAN = ['Januari','Februari','Maret','April','Mei','Juni','Juli','Agustus','September','Oktober','November','Desember'][today.getMonth()];
const TANGGAL = `${HARI}, ${today.getDate()} ${BULAN} ${today.getFullYear()}`;

const THREADS = [
  { author:'Sari Lestari',   chapter:'Jakarta',     title:'RUU PPRT, kenapa mandek terus setelah 20 tahun?', replies:42, time:'2j', tags:['Politik','Ketenagakerjaan'] },
  { author:'Kanta Widodo',   chapter:'Malang Raya', title:'Tarif parkir naik tanpa konsultasi warga — pengalaman dari Malang', replies:28, time:'4j', tags:['Lokal','Transportasi'] },
  { author:'Nadira Azzahra', chapter:'Bandung Raya',title:'Mental health di tempat kerja garmen Bandung: cerita yang jarang didengar', replies:67, time:'6j', tags:['Mental Health','Gender'] },
];

const KARYA = [
  { type:'Tulisan',    author:'Reza Adipratama', title:'Lima Alasan Pemuda Masih Apatis terhadap Pemilu Lokal',            meta:'7 mnt baca',    color:'#E8F0FE' },
  { type:'Vlog',       author:'Mei Chandra',     title:'Ngobrol sama Ibu PKL soal APBD — ternyata mereka lebih paham',     meta:'12:34',         color:'#FEE8E1' },
  { type:'Ilustrasi',  author:'Pram Faisal',     title:'Kenapa Suara Kita Bisa Hilang — dalam 6 panel visual',             meta:'Galeri',        color:'#E1F2EC' },
];

const KELAS_CARDS = [
  { title:'Kelas Jubir Warga: dari Resah ke Suara ke Aksi', peserta:124, progress:68, mentor:'Bilal Sukarno'       },
  { title:'Social Marketing & Fundraising untuk Gerakan',   peserta:89,  progress:34, mentor:'Aqidatul Izza Zain' },
];

function PageBeranda({ onNavigate }) {
  const [wordleOpen, setWordleOpen] = useBState(false);

  return (
    <div>

      {/* ── HERO ── */}
      <section className="py-12 md:py-16 border-b" style={{ borderColor: CB.line }}>
        <div className="max-w-6xl mx-auto px-6">
          <div className="flex flex-col md:flex-row items-start md:items-center gap-10">

            <div className="flex-1">
              <span className="font-hand text-lg" style={{ color: CB.coral }}>{TANGGAL}</span>
              <h1 className="font-display font-bold mt-2 leading-none" style={{ color: CB.blue, fontSize: 'clamp(44px,7vw,80px)' }}>
                Hari ini,<br />
                <em>kita ngomongin</em><br />
                <span className="squiggly">Pasal 28E.</span>
              </h1>
              <p className="mt-4 text-base md:text-lg max-w-md leading-relaxed" style={{ color: CB.ink + '99' }}>
                Hak berekspresi yang dijamin konstitusi — tapi seberapa jauh sudah dipraktikkan?
              </p>
              <div className="mt-6 flex flex-wrap gap-3">
                <BBtn variant="coral" size="lg" onClick={() => onNavigate('komunitas')}>Ikut diskusi →</BBtn>
                <BBtn variant="outline" size="lg" onClick={() => onNavigate('kelas')}>Baca lebih dalam</BBtn>
              </div>
            </div>

            {/* Illustration placeholder */}
            <div
              className="flex-shrink-0 w-56 md:w-72 rounded-3xl flex flex-col items-center justify-center relative overflow-hidden"
              style={{ background: CB.blue + '0E', border: `2px dashed ${CB.line}`, aspectRatio:'4/3' }}
            >
              <svg width="72" height="72" viewBox="0 0 72 72" fill="none">
                <circle cx="36" cy="24" r="14" fill={CB.blue} opacity=".14"/>
                <rect x="16" y="42" width="40" height="24" rx="7" fill={CB.blue} opacity=".10"/>
                <rect x="22" y="48" width="28" height="3" rx="1.5" fill={CB.blue} opacity=".35"/>
                <rect x="22" y="54" width="20" height="3" rx="1.5" fill={CB.blue} opacity=".25"/>
                <rect x="22" y="60" width="24" height="3" rx="1.5" fill={CB.blue} opacity=".18"/>
              </svg>
              <span className="font-mono text-xs mt-2 px-4 text-center" style={{ color: CB.blue + '55' }}>warga baca dokumen</span>
              <span
                className="font-hand text-sm absolute top-2 right-3"
                style={{ color: CB.coral, transform: 'rotate(4deg)' }}
              >← kamu juga bisa!</span>
            </div>
          </div>
        </div>
      </section>

      {/* ── MINI GAME ── */}
      <section className="py-10 border-b" style={{ borderColor: CB.line }}>
        <div className="max-w-6xl mx-auto px-6">
          <div className="grid md:grid-cols-3 gap-5">

            {/* Game card */}
            <div className="md:col-span-2 rounded-2xl p-6 relative overflow-hidden" style={{ background: CB.blue }}>
              <div className="flex items-start justify-between mb-4">
                <div>
                  <span className="font-hand text-lg" style={{ color: CB.marigold }}>Game Hari Ini</span>
                  <h3 className="font-display text-2xl font-bold text-white mt-1">Tebak Kata Hari Ini</h3>
                  <p className="text-sm mt-1" style={{ color: 'rgba(255,250,238,.6)' }}>Tebak 5 huruf yang berhubungan dengan kewargaan. 6 percobaan.</p>
                </div>
                <span
                  className="font-hand text-sm text-right"
                  style={{ color: 'rgba(255,250,238,.45)', transform: 'rotate(1.5deg)', marginTop: 2 }}
                >← coba dulu,<br />baru ngomong</span>
              </div>

              {/* Mini grid preview */}
              <div className="flex gap-2 mb-5">
                {['W','A','R','G','A'].map((l, i) => (
                  <div
                    key={i}
                    className="rounded-lg flex items-center justify-center font-display font-bold text-white"
                    style={{ width: 40, height: 40, fontSize: 20, background: CB.mint }}
                  >{l}</div>
                ))}
              </div>

              <div className="flex items-center gap-4 flex-wrap">
                <BBtn variant="coral" onClick={() => setWordleOpen(true)}>Main hari ini →</BBtn>
                <div className="flex items-center gap-1.5">
                  <span>🔥</span>
                  <span className="font-mono font-bold" style={{ color: CB.marigold }}>5</span>
                  <span className="text-sm" style={{ color: 'rgba(255,250,238,.55)' }}>hari streak</span>
                </div>
              </div>
            </div>

            {/* Leaderboard */}
            <div className="rounded-2xl p-5 border" style={{ borderColor: CB.line, background: '#fff' }}>
              <h4 className="font-semibold text-sm mb-4" style={{ color: CB.blue }}>🏆 Top 3 hari ini</h4>
              {[
                { name:'Sari Lestari',   chapter:'Jakarta',     score:'2/6', rank:0 },
                { name:'Kanta Widodo',   chapter:'Malang',      score:'3/6', rank:1 },
                { name:'Aulia Pratiwi',  chapter:'Bandung',     score:'4/6', rank:2 },
              ].map((p, i) => (
                <div key={i} className="flex items-center gap-3 py-2 border-b last:border-0" style={{ borderColor: CB.line }}>
                  <span className="text-base w-5 text-center">{['🥇','🥈','🥉'][i]}</span>
                  <BAvatar name={p.name} size="sm" showLevel={false} />
                  <div className="flex-1 min-w-0">
                    <div className="text-xs font-semibold truncate" style={{ color: CB.blue }}>{p.name.split(' ')[0]}</div>
                    <div className="text-[10px]" style={{ color: CB.ink + '55' }}>{p.chapter}</div>
                  </div>
                  <span className="font-mono text-xs font-bold" style={{ color: CB.coral }}>{p.score}</span>
                </div>
              ))}
            </div>
          </div>
        </div>
      </section>

      {/* ── THREE COLUMNS ── */}
      <section className="py-10 border-b" style={{ borderColor: CB.line }}>
        <div className="max-w-6xl mx-auto px-6">
          <div className="grid md:grid-cols-3 gap-8">

            {/* Forum */}
            <div>
              <h3 className="font-display font-bold text-lg mb-4 flex items-center gap-2" style={{ color: CB.blue }}>
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
                  <polyline points="23 6 13.5 15.5 8.5 10.5 1 18"/><polyline points="17 6 23 6 23 12"/>
                </svg>
                Lagi Hangat di Forum
              </h3>
              <div className="space-y-3">
                {THREADS.map((t, i) => (
                  <button key={i} onClick={() => onNavigate('komunitas')}
                    className="w-full text-left p-4 rounded-xl border card-lift"
                    style={{ borderColor: CB.line, background: '#fff' }}
                  >
                    <div className="flex items-center gap-2 mb-2">
                      <BAvatar name={t.author} size="sm" showLevel={false} />
                      <span className="text-xs font-semibold" style={{ color: CB.blue }}>{t.author}</span>
                      <span className="text-xs" style={{ color: CB.ink + '55' }}>· {t.chapter}</span>
                    </div>
                    <p className="font-display font-semibold text-sm leading-snug mb-2 clamp2" style={{ color: CB.ink }}>{t.title}</p>
                    <div className="flex items-center gap-3 text-xs mb-2" style={{ color: CB.ink + '55' }}>
                      <span>💬 {t.replies}</span><span>{t.time} lalu</span>
                    </div>
                    <div className="flex gap-1.5 flex-wrap">
                      {t.tags.map(tag => <BPill key={tag} color="grey">{tag}</BPill>)}
                    </div>
                  </button>
                ))}
              </div>
            </div>

            {/* Karya */}
            <div>
              <h3 className="font-display font-bold text-lg mb-4 flex items-center gap-2" style={{ color: CB.blue }}>
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
                  <path d="M12 20h9"/><path d="M16.5 3.5a2.121 2.121 0 0 1 3 3L7 19l-4 1 1-4z"/>
                </svg>
                Karya Terbaru
              </h3>
              <div className="space-y-3">
                {KARYA.map((k, i) => (
                  <button key={i} onClick={() => onNavigate('karya')}
                    className="w-full text-left p-4 rounded-xl border card-lift"
                    style={{ borderColor: CB.line, background: '#fff' }}
                  >
                    <div className="flex items-center gap-2 mb-2">
                      <BPill color={k.type==='Vlog'?'coral':k.type==='Ilustrasi'?'mint':'blue'}>{k.type}</BPill>
                      <span className="text-xs" style={{ color: CB.ink + '55' }}>{k.meta}</span>
                    </div>
                    <p className="font-display font-semibold text-sm leading-snug mb-2 clamp2" style={{ color: CB.ink }}>{k.title}</p>
                    <span className="text-xs" style={{ color: CB.ink + '55' }}>{k.author}</span>
                  </button>
                ))}
              </div>
            </div>

            {/* Aksi */}
            <div>
              <h3 className="font-display font-bold text-lg mb-4 flex items-center gap-2" style={{ color: CB.blue }}>
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
                  <polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/>
                </svg>
                Aksi Minggu Ini
              </h3>
              <button onClick={() => onNavigate('aksi')}
                className="w-full text-left p-4 rounded-xl border card-lift mb-3"
                style={{ borderColor: CB.coral + '55', background: CB.coral + '08' }}
              >
                <BPill color="coral" className="mb-2">POLLING</BPill>
                <p className="font-semibold text-sm mb-3" style={{ color: CB.ink }}>
                  Anggaran subsidi BBM sebaiknya dialihkan ke mana?
                </p>
                <BBtn variant="coral" size="sm" onClick={e => { e.stopPropagation(); onNavigate('aksi'); }}>
                  Vote sekarang →
                </BBtn>
              </button>
              <button onClick={() => onNavigate('aksi')}
                className="w-full text-left p-4 rounded-xl border card-lift"
                style={{ borderColor: CB.line, background: '#fff' }}
              >
                <BPill color="mint" className="mb-2">PETISI</BPill>
                <p className="text-sm font-semibold mb-2" style={{ color: CB.ink }}>Kembalikan jam KRL 04.00 WIB</p>
                <BProgress percent={73} colorKey="mint" />
                <p className="text-xs mt-1" style={{ color: CB.ink + '55' }}>7.300 / 10.000 tanda tangan</p>
              </button>
            </div>
          </div>
        </div>
      </section>

      {/* ── TANYA NALA STRIP ── */}
      <section className="py-10 border-b" style={{ borderColor: CB.line }}>
        <div className="max-w-6xl mx-auto px-6">
          <div
            className="rounded-3xl p-6 md:p-8 flex flex-col md:flex-row items-center gap-6"
            style={{ background: CB.blue }}
          >
            <div className="flex-shrink-0">
              {window.NalaMascot && <window.NalaMascot expression="excited" size={130} />}
            </div>
            <div className="flex-1 text-center md:text-left">
              <span className="font-hand text-base" style={{ color: CB.marigold }}>✦ Tanya Nala</span>
              <h3
                className="font-display font-bold mt-1 leading-tight"
                style={{ color: CB.cream, fontSize: 'clamp(22px,3vw,30px)' }}
              >
                <em>Beo curhatmu</em> — dengar kabar, ngasih tahu, sering tanya balik.
              </h3>
              <p className="text-sm mt-2" style={{ color: 'rgba(255,250,238,0.8)' }}>
                Selalu pakai sumber. Bilang "aku belum tahu" kalau memang belum tahu.
              </p>
            </div>
            <div className="flex-shrink-0">
              <BBtn variant="coral" size="lg" onClick={() => onNavigate('nala')}>
                Ngobrol bareng Nala →
              </BBtn>
            </div>
          </div>
        </div>
      </section>

      {/* ── KELAS ── */}
      <section className="py-10 border-b" style={{ borderColor: CB.line }}>
        <div className="max-w-6xl mx-auto px-6">
          <h3 className="font-display font-bold text-xl mb-5 flex items-center gap-2" style={{ color: CB.blue }}>
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
              <path d="M2 3h6a4 4 0 0 1 4 4v14a3 3 0 0 0-3-3H2z"/><path d="M22 3h-6a4 4 0 0 0-4 4v14a3 3 0 0 1 3-3h7z"/>
            </svg>
            Kelas yang Sedang Seru
          </h3>
          <div className="grid md:grid-cols-2 gap-4">
            {KELAS_CARDS.map((k, i) => (
              <button key={i} onClick={() => onNavigate('kelas')}
                className="text-left p-5 rounded-2xl border card-lift"
                style={{ borderColor: CB.line, background: '#fff' }}
              >
                <div className="flex items-start justify-between mb-3 gap-3">
                  <p className="font-display font-semibold leading-snug" style={{ color: CB.blue }}>{k.title}</p>
                  <BPill color="mint">LIVE</BPill>
                </div>
                <p className="text-xs mb-3" style={{ color: CB.ink + '66' }}>👤 {k.mentor} · 👥 {k.peserta} peserta</p>
                <BProgress percent={k.progress} colorKey="coral" />
                <p className="text-xs mt-1" style={{ color: CB.ink + '55' }}>{k.progress}% selesai</p>
              </button>
            ))}
          </div>
        </div>
      </section>

      {/* ── CTA ── */}
      <section className="py-16" style={{ background: CB.blue }}>
        <div className="max-w-6xl mx-auto px-6 text-center">
          <h2 className="font-display text-3xl md:text-4xl font-bold mb-3" style={{ color: CB.cream }}>
            Belum daftar?
          </h2>
          <p className="text-base mb-6" style={{ color: CB.cream + '77' }}>
            Daftar gratis, dapat KTP Warga Digital.
          </p>
          <BBtn variant="coral" size="lg" onClick={() => onNavigate('profil')}>
            Daftar gratis sekarang →
          </BBtn>
        </div>
      </section>

      {/* ── WORDLE MODAL ── */}
      {wordleOpen && (
        <div className="fixed inset-0 z-50 flex items-center justify-center p-4">
          <div className="absolute inset-0 backdrop-blur-sm" style={{ background: CB.blue + '77' }}
            onClick={() => setWordleOpen(false)} />
          <div className="relative rounded-2xl shadow-2xl w-full overflow-y-auto" style={{ background: CB.cream, maxWidth: 440, maxHeight: '92vh' }}>
            <button
              onClick={() => setWordleOpen(false)}
              className="absolute top-3 right-3 z-10 w-8 h-8 rounded-full flex items-center justify-center text-xs font-bold"
              style={{ background: CB.line, color: CB.ink }}
            >✕</button>
            <CitizenWordle
              onOpenPage={p => { setWordleOpen(false); onNavigate(p); }}
            />
          </div>
        </div>
      )}
    </div>
  );
}

Object.assign(window, { PageBeranda });
